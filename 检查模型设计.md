# 检查模型设计

本文是 TinyGateway reviewer 检查模型的设计说明。更完整的产品语义见 [技术说明.md](./技术说明.md)；本文聚焦 reviewer 的输入、输出和执行边界。

## 核心原则

TinyGateway 不做机械审查。

旧的关键词、路径、命令、正则类检查不作为产品主线，也不作为兜底规则保留。`dangerous_shell`、`secret_exposure` 等只能作为 reviewer 输出的分类标签，不是本地规则。

网关负责：

- 收集请求、响应和上下文；
- 构造结构化 ReviewJob；
- 调用 reviewer model；
- 校验 reviewer 输出；
- 按结构化结果执行 allow、audit、confirm、block、redact；
- 保存审计日志。

Reviewer model 负责判断风险。

核心边界：

```text
网关不判断内容是否危险；网关只执行 reviewer 返回的结构化决策。
```

## 不做客户端提示词特化

TinyGateway 不针对 Claude Code、Codex 或其他客户端做提示词特化，也不为了某个客户端随意改写正常模型正文。

可以做：

- API 协议适配：Anthropic 风格 `/v1/messages`、OpenAI 风格 `/v1/chat/completions`；
- ReviewJob 统一化：把不同协议的请求/响应整理成统一审查输入；
- 协议兼容错误返回：block、confirm timeout 等错误按客户端协议返回。

不建议做：

- 根据客户端类型修改正常模型回复；
- 把警告文本塞进 Claude Code / Codex 的模型输出正文；
- 通过提示词格式 hack 让客户端表现不同。

唯一例外是 reviewer 明确返回 `redact` 且提供结构化 redactions 时，网关可以做受控脱敏改写。

## 审查语义

审查语义应拆成两个维度，而不是把 `full` 当成和 `audit`、`guard` 同级的长期模式。

```text
执行模式：audit / guard
上下文范围：response-only / full-context
```

### 执行模式

`off`：关闭检查模型，只做模型网关、provider 路由、模型映射和基础审计记录。

`audit`：被动检查。不阻断请求，不影响工作流。即使 reviewer 返回 `block`、`confirm` 或 `redact`，网关也只写审计。

`guard`：主动执行 reviewer 决策。Reviewer 返回 `confirm`、`block` 或 `redact` 时，网关可以要求确认、阻断或脱敏。

### 上下文范围

`response-only`：reviewer 主要看模型响应。适合 inbound guard MVP。

`full-context`：reviewer 同时看客户端请求、用户意图、系统提示、工具上下文和模型响应。

`full-context` 只是表示 reviewer 可见上下文更多，不表示网关一定更激进拦截。`audit` 和 `guard` 都应该能开启 full-context。

### 出站审查

出站审查不应混在 `full` 这个单一 mode 里，建议单独配置：

```text
outboundReview:
  off    不审查出站请求
  audit  审查出站请求但只记录
  guard  出站请求也可 confirm/block/redact
```

出站审查用于判断客户端请求和上下文是否允许发给上游模型，尤其用于防止敏感上下文外发。

## 推荐 Reviewer 配置方向

当前代码仍保留旧配置形态。后续应收敛为：

```json
{
  "reviewer": {
    "enabled": true,
    "mode": "audit",
    "context": "response",
    "outboundReview": "off",
    "provider": "local-reviewer",
    "model": "qwen2.5-coder:7b",
    "timeoutMs": 12000,
    "failBehavior": "allow",
    "confirmBehavior": "hold"
  }
}
```

字段：

- `enabled`：是否启用 reviewer。
- `mode`：`audit` / `guard`，控制是否打断客户端。
- `context`：`response` / `full`，控制 reviewer 看多少上下文。
- `outboundReview`：`off` / `audit` / `guard`，控制请求发给上游前是否审查。
- `provider`：reviewer 使用的 provider。
- `model`：reviewer 使用的模型。
- `timeoutMs`：审查超时。
- `failBehavior`：reviewer 失败时的处理方式，支持 `allow` / `audit` / `block`。
- `confirmBehavior`：reviewer 返回 `confirm` 时的处理方式，建议支持 `hold` / `retry` / `block` / `allow`。

## 审查输入

Reviewer 不应该只看原始 prompt。网关应该把上下文整理成结构化 ReviewJob：

```json
{
  "id": "review_xxx",
  "requestId": "req_xxx",
  "mode": "guard",
  "context": "full",
  "direction": "inbound",
  "endpoint": "/v1/messages",
  "clientProtocol": "anthropic",
  "provider": "anthropic-main",
  "localModel": "sonnet",
  "upstreamModel": "claude-sonnet-4-5-20250929",
  "stream": false,
  "request": {
    "model": "sonnet",
    "system": "...",
    "messages": [],
    "tools": []
  },
  "response": {
    "statusCode": 200,
    "content": [],
    "toolCalls": [],
    "rawText": "..."
  },
  "metadata": {
    "createdAt": "...",
    "truncated": false
  }
}
```

方向：

- `outbound`：客户端请求准备发给上游模型。
- `inbound`：上游响应准备返回客户端。

ReviewJob 不应包含 provider API key。

## 审查输出

Reviewer 必须输出 JSON。字段名和 `risk` / `action` / `categories` 枚举值保持英文，便于网关稳定解析；`reason`、`evidence`、`suggestedUserPrompt` 和 `redactions.replacement` 面向用户展示，必须使用中文。

```json
{
  "risk": "medium",
  "action": "confirm",
  "confidence": 0.82,
  "categories": [
    "sensitive_file_access"
  ],
  "reason": "The response suggests reading a local secret file before continuing.",
  "evidence": [],
  "requiresUserApproval": true,
  "suggestedUserPrompt": "The model wants to read a sensitive local file. Allow this action?",
  "redactions": []
}
```

风险等级：

```text
low
medium
high
critical
```

动作：

```text
allow
audit
confirm
block
redact
```

分类：

```text
prompt_injection
secret_exposure
system_prompt_extraction
sensitive_file_access
dangerous_shell
network_exfiltration
dependency_risk
git_risk
cloud_risk
privacy_risk
policy_bypass
unknown
```

## confirm 语义

`confirm` 不应只理解成错误码。

### retry confirmation

当前基础实现：

```text
reviewer 返回 confirm
-> 网关返回 409 confirmation_required + confirmationId
-> 用户在管理端 allow/block
-> 客户端带 x-tinygateway-confirmation: <confirmationId> 重试
```

这适合可控 API 客户端，但不适合 Claude Code / Codex 默认体验。

### hold confirmation

后续推荐实现：

```text
reviewer 返回 confirm
-> 网关挂起原 HTTP 请求
-> Web Admin / 托盘显示确认项
-> 用户 allow
-> 原请求继续返回
```

用户 block 或确认超时，则原请求返回协议兼容错误。

## redact 语义

`redact` 只执行 reviewer 明确返回的结构化脱敏建议。

第一阶段只支持 exact text replacement：

```json
{
  "action": "redact",
  "redactions": [
    {
      "text": "sk-live-xxx",
      "replacement": "[REDACTED_KEY]"
    }
  ]
}
```

网关不自行推断更多脱敏项。

## streaming 限制

streaming 当前只适合透传 + audit capture。

如果流式响应已经开始发给客户端，再做同步阻断就太晚。因此短期应明确：

```text
streaming 当前不支持真正 guard/full 同步拦截。
```

未来可选方案：

- 只做 streaming audit；
- 高安全模式强制 non-stream；
- 缓冲完整响应后审查；
- 分段缓冲和分段审查。

## 本地模型优先

如果 full-context 或 outbound review 把准备发给上游的信息再交给另一个云端 reviewer，隐私边界会变差。

推荐：

- audit：可以用云端或本地 reviewer；
- guard：优先本地 reviewer；
- full-context + outboundReview：强烈建议本地 reviewer。

可接 Ollama、LM Studio、LocalAI，或任意 OpenAI-compatible 本地服务。

## 实施顺序

1. audit：观察真实流量，只记录不打断。
2. guard inbound：non-stream 响应同步审查，支持 allow/audit/confirm/block/redact。
3. retry confirmation：管理端确认 + 客户端 header 重试。
4. 语义收敛：把旧 `full` 拆成 `context` 与 `outboundReview`。
5. hold confirmation：挂起原请求，用户确认后继续，适配 Claude Code / Codex。
6. outbound guard：confirm/redact 不再静默放行。
7. streaming guard：单独设计缓冲或分段审查，不作为当前 MVP 主线。
