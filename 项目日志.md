# 项目日志

## 2026-05-28 14:12 UTC — 攻击模拟参数化入口与 WebUI 配置

### 背景

在攻击模拟虚拟模型基础版之后，本轮补充用户要求的参数化能力：提供 `attack-sim` 通用入口，并允许管理员控制客户端是否能传入 `scenario`、`intensity`、`insertionStyle`。

### 1. TDD 覆盖

新增和更新测试覆盖：

- `test/attack-simulator.test.js`：`attackSimulation` 通用策略归一化；`GET /v1/models` 暴露 `client_parameterized` 和允许列表；固定场景路由补齐默认参数字段。
- `test/server-audit-integration.test.js`：通用 `attack-sim` 接收允许的客户端参数并写入场景提示词；越界场景在调用上游前返回 400。
- `test/admin-reviewer-ui.test.js`：Admin UI 暴露 `allowClientParams`、`allowedScenarios`、`allowedIntensities`、`allowedInsertionStyles`、默认强度和默认插入方式。

聚焦验证：

```bash
node --check src/admin-ui/app.js
node --test test/admin-reviewer-ui.test.js test/attack-simulator.test.js test/server-audit-integration.test.js
```

结果：通过。

### 2. 实现摘要

成果：

- `src/config.js`：新增 `attackSimulation` 通用配置归一化；`attackSimulators` 固定路由补齐 `allowClientParams`、强度、插入方式和 `safeMode`。
- `src/models.js`：启用通用攻击模拟时在模型列表暴露 `attack-sim`，并返回客户端参数化元数据。
- `src/attack-simulator.js`：解析 `tinygateway.attackSimulation`，按允许列表校验 `scenario` / `intensity` / `insertionStyle`，包装上游请求前移除 `tinygateway` 元数据。
- `src/server.js`：攻击模拟审计补充 `intensity`、`insertionStyle`、`safeMode`、`clientParameterized`。
- `src/admin-ui/index.html`、`src/admin-ui/app.js`、`src/admin-ui/app.css`：攻击模拟页补充通用参数化配置面板，并保留固定场景路由配置。
- `config.json`：新增默认禁用的 `attackSimulation` 通用入口示例；固定路由示例补齐参数字段。

### 3. 安全边界

- 客户端参数只在 `allowClientParams=true` 时生效。
- 客户端只能选择管理员允许列表中的场景、强度和插入方式；越界直接 400，不调用上游。
- 客户端不能传任意攻击提示词；`tinygateway` 元数据不会转发给 provider。
- 场景提示词仍强制要求抽象模拟，禁止真实命令、脚本、路径、凭据、漏洞利用代码或绕过步骤。

### 4. 文档更新

同步更新 README、技术说明、规划和本项目日志，明确 `attackSimulation` / `attackSimulators` 两类入口、客户端参数白名单和审计字段。

## 2026-05-28 10:24 UTC — 攻击模拟虚拟模型基础版

### 背景

本轮在 reviewer / guard / hold confirmation 已收口的基础上，实现用户提出的“攻击模拟模型”设想：提供一个一直存在/可配置的特殊虚拟模型路由，客户端可像调用普通模型一样调用它；网关用选定上游模型根据预置攻击模拟场景生成安全抽象的模拟响应，再让响应走常规 reviewer / guard 链路，用于验证防护效果。

### 1. TDD 覆盖

新增和更新测试覆盖：

- `test/attack-simulator.test.js`：`attackSimulators` 配置归一化、未知 provider 校验、`GET /v1/models` 暴露启用的攻击模拟虚拟模型。
- `test/server-audit-integration.test.js`：攻击模拟虚拟模型会包装请求为场景提示词并调用配置的真实上游模型；模拟响应继续经过 inbound guard，可被现有 reviewer block。
- `test/admin-reviewer-ui.test.js`：Admin UI 暴露攻击模拟配置页、场景选择和保存逻辑。

聚焦验证：

```bash
node --test --test-name-pattern "attack simulator" test/attack-simulator.test.js test/server-audit-integration.test.js
node --test test/admin-reviewer-ui.test.js test/attack-simulator.test.js
```

结果：通过。

### 2. 实现摘要

成果：

- `src/attack-simulator.js`：新增攻击模拟提示词构建模块，内置 `startup_poisoning`、`prompt_injection`、`data_exfiltration` 三个安全抽象场景。
- `src/config.js`：新增 `attackSimulators` 配置解析、校验、归一化；启用项会进入 `buildModelIndex()`。
- `src/models.js`：`GET /v1/models` 暴露启用的攻击模拟虚拟模型，标记 `type: "attack_simulator"`。
- `src/server.js`：命中攻击模拟模型时，使用场景提示词包装原请求，调用配置的真实 provider/model；写入 `attack_simulation` 审计事件；模拟响应继续走原有 inbound reviewer / guard。
- `src/admin-ui/index.html`、`src/admin-ui/app.js`：新增“攻击模拟”页，支持新增、保存、删除、启用/禁用攻击模拟路由，并选择 provider、上游模型和场景。
- `config.json`：新增默认禁用的 `attack-sim/startup-poisoning` 示例配置。

### 3. 安全边界

- 攻击模拟不是任意攻击生成器，普通客户端不能传入任意攻击提示词。
- 只允许调用管理员配置的 `attack-sim/...` 场景。
- 场景提示词要求输出抽象模拟内容，不输出真实命令、脚本、注册表路径、启动目录路径、下载地址、凭据、漏洞利用代码或绕过步骤。
- 高风险行为必须用 `[模拟的...]` 占位符表达。
- 模拟响应必须继续经过 reviewer / guard；如果 reviewer 返回 block/confirm/redact，网关照常执行。

### 4. 文档更新

同步更新 README、技术说明和规划：

- README 增加攻击模拟虚拟模型配置、调用方式、内置场景和审计说明。
- 技术说明增加“攻击模拟虚拟模型”章节，明确链路和安全边界。
- 规划新增 Phase 9：攻击模拟虚拟模型基础版，并把 tray-only 控制器后移为 Phase 10。

### 5. 剩余缺口

- 攻击模拟测试报告/批量回归尚未实现。
- 不同 reviewer 模型防护效果对比尚未实现。
- 自定义场景的管理员审核/导入导出尚未实现。

## 2026-05-28 09:21 UTC — P3 hold confirmation 基础版

### 背景

本轮目标是完成 Phase 7 / P3：`confirmBehavior: "hold"` 下不再要求客户端读取 `confirmationId` 并带 header 重试，而是挂起原 HTTP 请求，等待 Web Admin allow/block 后自动继续或返回错误，更适配 Claude Code / Codex。

### 1. TDD 覆盖

新增和更新测试覆盖：

- `test/confirmation-queue.test.js`：`waitForResolution()` 等待 allow/block，并支持超时返回 `timeout`。
- `test/config.test.js`：`holdTimeoutMs` 默认值、非法值归一化、旧 full 配置运行时兼容。
- `test/admin-reviewer-ui.test.js`：Reviewer 表单展示并保存 `holdTimeoutMs`。
- `test/server-audit-integration.test.js`：inbound hold allow 后返回原上游响应；inbound hold block 返回 `confirmation_blocked`；inbound hold timeout 返回 `confirmation_timeout`；outbound hold allow 前不调用上游、allow 后再调用上游；outbound hold timeout 不调用上游。

聚焦验证：

```bash
npm test -- --test-name-pattern="hold|ConfirmationQueue|reviewer admin form|parseConfig normalizes reviewer holdTimeoutMs"
```

结果：通过。

### 2. 实现摘要

成果：

- `src/confirmations.js`：确认队列增加 waiter；`waitForResolution(id, { timeoutMs })` 可等待管理端决议；`resolve()` 会通知挂起请求；超时清理 waiter。
- `src/server.js`：inbound `confirmBehavior: "hold"` 会暂存上游响应并等待确认；allow 后返回原响应；block 返回 `confirmation_blocked`；超时返回 `confirmation_timeout`。
- `src/server.js`：outbound `confirmBehavior: "hold"` 会在发往上游前挂起；allow 后才调用上游；block/timeout 均不会调用上游。
- `src/server.js`：审计补齐 `confirmation_used`、`confirmation_blocked`、`confirmation_timeout`，并区分 inbound/outbound。
- `src/config.js`：新增并归一化 `reviewer.holdTimeoutMs`，默认 120000 ms，非法/非正数回落默认值。
- `src/admin-ui/index.html`、`src/admin-ui/app.js`：Reviewer 配置页新增 Hold timeout 输入框，并在读取/保存配置时带上 `holdTimeoutMs`。
- `config.json`：示例配置新增 `reviewer.holdTimeoutMs`。

### 3. 文档更新

同步更新 README、技术说明和规划：

- README 当前能力加入 hold confirmation；确认队列说明区分 `hold` 与 `queue/retry`。
- 技术说明补充 `holdTimeoutMs`、inbound/outbound hold 行为和审计事件。
- 规划将 Phase 7 标记为已实现基础版，并从剩余缺口中移除 hold confirmation。

### 4. 剩余缺口

- outbound 请求体 redaction 实际改写后再调用上游仍未实现。
- streaming 仍不支持同步 guard。
- tray-only 控制器仍未实现。

### 5. 验证

聚焦验证已通过；全量 `npm test` 待本轮最终验证后补充结果。

