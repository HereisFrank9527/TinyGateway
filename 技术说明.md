# TinyGateway 技术说明

本文用于统一 TinyGateway 的产品语义、审查模型边界和后续实现方向。它不是代码完成状态声明；如果与 README 或旧项目日志冲突，以本文的概念边界作为后续收敛方向。

## 1. 项目定位

TinyGateway 是一个本地 LLM 网关，目标是在本地统一管理：

- AI 编程客户端的 base URL 接入；
- provider、API key、模型映射；
- 请求和响应审计；
- reviewer 检查模型；
- 高风险动作确认；
- 攻击模拟虚拟模型；
- 本地 Web 管理端。

TinyGateway 不应该变成一个“桌面大 App”。当前更合适的形态是：

```text
后台本地网关服务 + 浏览器 Web Admin + 系统托盘轻量控制器
```

Web Admin 继续作为主要管理界面。系统托盘只负责启动、停止、重启、打开 Web Admin、查看日志和开机自启等轻控制能力。

## 2. 不做机械审查

TinyGateway 不保留机械审查主线。

不应该在网关本地实现以下风险判断链路：

- 关键词拦截；
- 命令正则拦截；
- 路径规则拦截；
- 域名规则拦截；
- 基于字符串模式的风险打分；
- 类似 `evaluatePayload` 的本地规则检查链。

`dangerous_shell`、`secret_exposure` 等分类只能作为 reviewer 检查模型返回的分类标签，而不是本地正则或关键词规则。

核心原则：

```text
网关不判断内容是否危险；网关只执行 reviewer 返回的结构化决策。
```

## 3. 不做客户端提示词特化

TinyGateway 不应该针对 Claude Code、Codex 或其他客户端做“提示词特化”。

这里要区分三件事。

### 3.1 可以做：协议适配

不同客户端使用不同 API 协议：

- Claude Code 常见为 Anthropic 风格 `POST /v1/messages`；
- OpenAI SDK 常见为 OpenAI 风格 `POST /v1/chat/completions`。
- Codex 新版本常见为 OpenAI Responses 风格 `POST /v1/responses`，TinyGateway 通过本地适配层转换到内部 chat/completions 路径。

TinyGateway 可以做协议兼容和 provider 路由，保证请求和响应格式符合客户端预期。

这是 API 协议适配，不是提示词特化。

### 3.2 可以做：ReviewJob 统一化

无论客户端来自 Anthropic 风格还是 OpenAI 风格，网关内部可以整理成统一的 ReviewJob：

```json
{
  "id": "review_xxx",
  "clientProtocol": "anthropic",
  "direction": "inbound",
  "request": {},
  "response": {},
  "metadata": {}
}
```

reviewer 只看统一后的 ReviewJob，并返回统一结构化结果。

这是检测输入统一化。

### 3.3 可以做：错误响应适配

当网关需要 block、confirm timeout 或返回其他错误时，应按客户端协议返回兼容错误。例如 Anthropic 客户端收到 Anthropic 风格错误，OpenAI 客户端收到 OpenAI 风格错误。

这是错误响应适配。

### 3.4 不建议做：改写正常模型正文

TinyGateway 不应该为了适配某个客户端，随意把模型正常输出改写成特定提示词或警告文本。

原因是 Claude Code、Codex 等工具会把这些文本当作模型正常输出处理，可能污染计划、代码生成或工具调用。

唯一例外是 reviewer 明确返回 `redact`，并给出结构化 redactions 时，网关可以做受控脱敏改写。

## 4. reviewer 实际调用

当前代码已经实现 reviewer provider 调用；如果其他文档仍把 reviewer 调用写成未完成，需要修正。

当前实现方向包括：

- OpenAI-compatible reviewer：调用 `/v1/chat/completions`；
- Anthropic reviewer：调用 `/v1/messages`；
- reviewer 请求强制 `stream: false`；
- OpenAI-compatible reviewer 可要求 JSON object 输出；
- reviewer 请求带 `x-tinygateway-reviewer: 1`，避免递归审查。

这部分可以作为 TinyGateway 的可吸收资产继续保留。

## 5. 审查语义应拆成两个维度

当前代码中出现过 `off / audit / guard / full` 这种单轴模式，但从产品语义看，`full` 不应该是和 `audit`、`guard` 同级的模式。

更合理的设计是拆成两个维度：

```text
执行模式：audit / guard
上下文范围：response-only / full-context
```

也就是说，`full` 更适合作为一个上下文标签，建议命名为 `full-context`。

## 6. execution mode：audit 与 guard

### 6.1 audit

`audit` 表示只审计，不打断客户端。

reviewer 可以返回风险、分类、理由和建议动作，但网关只记录，不阻断、不确认、不脱敏。

适用场景：

- 早期观察真实流量；
- 调试 reviewer prompt；
- 收集风险样本；
- 避免干扰 Claude Code、Codex 等客户端。

### 6.2 guard

`guard` 表示 reviewer 的结构化决策会影响请求或响应。

reviewer 可返回：

```text
allow   放行
audit   放行但记录
confirm 要求用户确认
block   阻断
redact  脱敏后放行
```

`guard` 才允许产生确认、阻断、脱敏等用户可见行为。

## 7. context mode：response-only 与 full-context

### 7.1 response-only

`response-only` 表示 reviewer 主要只看模型返回内容。

适合先做 inbound guard：

```text
客户端请求 -> 上游模型响应 -> reviewer 审查响应 -> 放行/确认/阻断/脱敏
```

### 7.2 full-context

`full-context` 表示 reviewer 不只看模型响应，也看客户端传入的请求、用户意图、系统提示、工具上下文等信息。

它的目标是让 reviewer 判断：

- 模型响应是否符合用户真实意图；
- 是否要执行或建议执行高风险动作；
- 是否存在越权操作；
- 是否在上下文中泄露敏感信息；
- 是否把本不该发出的上下文发给上游；
- 是否需要用户确认。

关键点：

```text
full-context 是“审查模型可见上下文更多”，不是“网关一定更激进拦截”。
```

因此 audit 和 guard 都应该能开启 full-context。

## 8. 推荐配置模型

建议未来从：

```json
{
  "mode": "audit" | "guard" | "full"
}
```

收敛为：

```json
{
  "reviewer": {
    "mode": "audit",
    "context": "response",
    "outboundReview": "off",
    "confirmBehavior": "hold"
  }
}
```

字段含义：

### 8.1 `mode`

控制 inbound 响应如何处理：

```text
audit  只记录，不打断
guard  允许 confirm/block/redact
```

### 8.2 `context`

控制 reviewer 看多少上下文：

```text
response  主要看响应
full      看请求 + 响应 + 可用上下文
```

也可以命名为：

```text
response-only / full-context
```

### 8.3 `outboundReview`

控制请求发给上游前是否审查：

```text
off    不审查出站请求
audit  审查出站请求但只记录
guard  出站请求也可 confirm/block/redact
```

### 8.4 `confirmBehavior`

控制 reviewer 返回 `confirm` 时怎么处理：

```text
hold   挂起原请求，用户确认后原请求继续
retry  返回 confirmationId，让客户端带 header 重试
queue  retry 的兼容别名，行为同 retry
block  直接阻断
allow  直接放行
```

`holdTimeoutMs` 控制 `hold` 等待用户确认的超时时间，默认 120000 ms。超时后原请求返回 `confirmation_timeout`，并写入审计日志。

## 9. 典型组合

### 9.1 观察模式

```json
{
  "reviewer": {
    "mode": "audit",
    "context": "full",
    "outboundReview": "audit"
  }
}
```

含义：请求和响应都给 reviewer 看，但只记录，不打断用户。

### 9.2 Claude Code 推荐默认

```json
{
  "reviewer": {
    "mode": "guard",
    "context": "full",
    "outboundReview": "audit",
    "confirmBehavior": "hold"
  }
}
```

含义：响应侧可以被确认、阻断、脱敏；请求侧先只审计，降低误打断。

### 9.3 高安全模式

```json
{
  "reviewer": {
    "mode": "guard",
    "context": "full",
    "outboundReview": "guard",
    "confirmBehavior": "hold"
  }
}
```

含义：请求发给模型前和模型响应返回客户端前都可能被确认、阻断或脱敏。

### 9.4 普通 API 客户端兼容模式

```json
{
  "reviewer": {
    "mode": "guard",
    "context": "full",
    "confirmBehavior": "retry"
  }
}
```

含义：不挂住 HTTP 请求，而是返回 confirmationId，让调用方自己决定何时重试。

## 10. inbound 与 outbound 要分开

### 10.1 inbound review

inbound review 审查的是上游模型准备返回给客户端的内容。

流程：

```text
客户端请求 -> 上游模型响应 -> 网关暂存响应 -> reviewer 审查 -> 放行/确认/阻断/脱敏 -> 返回客户端
```

用户确认的是：

```text
是否允许这段模型输出返回给客户端？
```

### 10.2 outbound review

outbound review 审查的是客户端准备发给上游模型的请求。

流程：

```text
客户端请求 -> 网关暂存请求 -> reviewer 审查 -> 放行/确认/阻断/脱敏 -> 发给上游模型
```

用户确认的是：

```text
是否允许这个请求和上下文发给上游模型？
```

outbound review 对防止敏感上下文外发尤其重要。

## 11. confirm 的产品语义

`confirm` 不应只理解成一个错误码。它有两种产品语义。

### 11.1 retry confirmation

当前已实现的基础方式：

```text
reviewer 返回 confirm
-> 网关返回 409 confirmation_required + confirmationId
-> 用户在管理端 allow/block
-> 客户端带 x-tinygateway-confirmation: <confirmationId> 重试
```

优点：实现简单，适合可控 API 客户端。

缺点：Claude Code、Codex 等工具未必知道如何自动重试，也不一定能带自定义 confirmation header。

### 11.2 hold confirmation

更适合 AI 编程工具的方式：

```text
reviewer 返回 confirm
-> 网关挂起原 HTTP 请求
-> Web Admin / 托盘显示确认项
-> 用户 allow
-> 原请求继续返回
```

已实现的行为：

- inbound hold：上游响应已暂存，allow 后返回原上游响应；block 返回 `confirmation_blocked`；等待超过 `holdTimeoutMs` 返回 `confirmation_timeout`。
- outbound hold：请求在发给上游前暂存，allow 后再调用上游；block/timeout 都不会调用上游。
- `ConfirmationQueue.waitForResolution()` 负责唤醒挂起请求；管理端 `/allow`、`/block` 会更新确认项并通知 waiter。
- 审计记录 `confirmation_created`、`confirmation_used`、`confirmation_blocked`、`confirmation_timeout`。

如果用户 block：

```text
原请求返回协议兼容错误
```

如果超时：

```text
原请求返回确认超时错误
```

这才更接近“确认后再把被拦截的内容送回客户端”。

## 12. 拦截时不要先给客户端塞警告文本

默认不建议在拦截时先把警告文本返回给 Claude Code、Codex 等客户端。

原因：客户端会把警告文本当作模型输出的一部分，可能污染后续推理、计划和代码生成。

推荐行为：

```text
确认信息显示在 Web Admin / 托盘通知中。
客户端原请求保持 pending；用户确认后，原请求继续或失败。
```

也就是说，拦截应是“请求级确认”，不是“正文里插入警告”。

## 13. block、confirm、redact 的返回策略

### 13.1 block

直接阻断，并返回协议兼容错误。

- OpenAI-compatible 客户端返回 OpenAI 风格错误；
- Anthropic-compatible 客户端返回 Anthropic 风格错误。

### 13.2 confirm

根据 `confirmBehavior` 决定：

- `hold`：挂起原请求；
- `retry`：返回 409 + confirmationId；
- `block`：直接阻断；
- `allow`：直接放行。

### 13.3 redact

`redact` 表示 reviewer 明确给出结构化脱敏建议。

推荐第一阶段只支持 exact text replacement：

```json
{
  "action": "redact",
  "redactions": [
    {
      "text": "sk-live-xxx",
      "replacement": "[REDACTED_KEY]"
    }
  ]
}
```

不要在网关本地自行推断更多脱敏项。

## 14. 攻击模拟虚拟模型

攻击模拟不是普通 provider，而是 TinyGateway 内置的虚拟模型路由。它用于验证网关防护效果，不用于生成可执行攻击材料。

当前支持两种配置入口：

1. `attackSimulation`：通用虚拟模型，默认 `modelId` 为 `attack-sim`。
2. `attackSimulators`：固定场景虚拟模型，例如 `attack-sim/startup-poisoning`。

### 14.1 通用入口 attackSimulation

配置示例：

```json
{
  "attackSimulation": {
    "enabled": true,
    "modelId": "attack-sim",
    "displayName": "通用攻击模拟",
    "provider": "openai-main",
    "model": "gpt-4.1-mini",
    "allowClientParams": true,
    "defaultScenario": "startup_poisoning",
    "allowedScenarios": ["startup_poisoning", "prompt_injection", "data_exfiltration"],
    "defaultIntensity": "low",
    "allowedIntensities": ["low", "medium", "high"],
    "defaultInsertionStyle": "obvious",
    "allowedInsertionStyles": ["obvious", "natural", "hidden"],
    "safeMode": "abstract"
  }
}
```

客户端可像调用普通模型一样调用 `attack-sim`。如果管理员启用 `allowClientParams`，客户端可以通过 `tinygateway.attackSimulation` 传入受控参数：

```json
{
  "model": "attack-sim",
  "messages": [
    { "role": "user", "content": "写一段软件安装说明" }
  ],
  "tinygateway": {
    "attackSimulation": {
      "scenario": "prompt_injection",
      "intensity": "medium",
      "insertionStyle": "natural"
    }
  }
}
```

约束：

- 只有 `allowClientParams=true` 时客户端参数才会生效。
- `scenario` 必须在 `allowedScenarios` 内。
- `intensity` 必须在 `allowedIntensities` 内。
- `insertionStyle` 必须在 `allowedInsertionStyles` 内。
- 任一参数越界都会在调用上游前返回 400。
- `tinygateway` 元数据只在网关本地使用，包装上游请求前会移除，不会转发给 provider。

### 14.2 固定场景入口 attackSimulators

配置示例：

```json
{
  "attackSimulators": [
    {
      "id": "attack-sim/startup-poisoning",
      "displayName": "启动项投毒模拟",
      "enabled": true,
      "provider": "openai-main",
      "model": "gpt-4.1-mini",
      "scenario": "startup_poisoning",
      "allowClientParams": false,
      "defaultIntensity": "low",
      "allowedIntensities": ["low", "medium", "high"],
      "defaultInsertionStyle": "obvious",
      "allowedInsertionStyles": ["obvious", "natural", "hidden"],
      "safeMode": "abstract"
    }
  ]
}
```

固定场景入口适合暴露稳定模型名。默认 `allowClientParams=false`，客户端不能改场景；如果管理员显式开启，也只能改强度和插入方式等白名单参数，不能传任意攻击提示词。

### 14.3 运行链路

```text
客户端请求 attack-sim 或 attack-sim/xxx
-> TinyGateway 识别虚拟模型
-> 读取默认场景或受控客户端参数
-> 校验 scenario / intensity / insertionStyle 是否在管理员允许列表内
-> 构造安全测试用攻击模拟提示词
-> 移除 tinygateway 元数据并调用配置的真实上游 provider/model
-> 获得模拟响应
-> 继续进入常规 inbound reviewer / guard
-> allow / block / redact / confirm / hold
-> 返回客户端
```

### 14.4 设计边界

- 攻击模拟只用于验证网关防护效果，不是攻击生成器。
- 普通客户端不能传入任意攻击提示词，只能调用管理员预置场景，或在管理员允许列表内调整 `scenario`、`intensity`、`insertionStyle`。
- 场景提示词要求输出抽象模拟内容，禁止真实命令、脚本、注册表路径、启动目录路径、下载地址、凭据、漏洞利用代码或绕过步骤。
- 高风险行为必须用 `[模拟的...]` 占位符表达。
- 模拟响应必须继续走 reviewer/guard；如果 reviewer 判定 block/confirm/redact，网关照常执行。
- 审计写入 `attack_simulation`，记录 `scenario`、`intensity`、`insertionStyle`、`clientParameterized`、虚拟模型、上游 provider/model 和 `requestId`。

当前内置场景：

```text
startup_poisoning   启动项投毒模拟
prompt_injection    提示词注入模拟
data_exfiltration   数据外传模拟
```

当前强度：

```text
low
medium
high
```

当前插入方式：

```text
obvious
natural
hidden
```

## 15. streaming 限制

streaming 是单独问题。

如果流式响应已经开始发给客户端，再想同步阻断就太晚了。

因此当前应明确：

```text
streaming 当前只支持透传 + audit capture，不支持真正 guard/full 同步拦截。
```

未来可选方案：

1. 禁用 streaming guard，只做 audit；
2. 缓冲完整响应后再审查，但会失去流式体验；
3. 分段缓冲和分段审查，但复杂度高，且可能破坏客户端体验；
4. 对高风险模式强制 non-stream。

短期建议：

```text
streaming guard 不作为 MVP 主线。
```

## 16. 当前实现状态判断

当前实现可以吸收和保留的部分：

- review 模块结构；
- reviewer provider 调用；
- OpenAI-compatible / Anthropic reviewer；
- `x-tinygateway-reviewer: 1` 防递归；
- ReviewJob 抽象；
- JSONL audit；
- guard 的 non-stream inbound 审查；
- confirmation queue 的基础数据结构；
- redact exact text 基础实现；
- Node 内置 test runner 测试体系。

已完成的收敛：

- README 中 reviewer 实际调用状态需要修正；
- `full` 不应作为完成态宣传，应降级为实验性；
- outbound review 的 `confirm` / `redact` 不能静默放行；

仍需收敛或重新标注的部分：

- confirmation retry 模式不适合作为 Claude Code 默认体验；
- streaming 不能宣传为 guard/full 可同步拦截；
- 中文文档如出现乱码，应统一 UTF-8 重写；
- 桌面端方向应从“内嵌 App”改成“系统托盘控制器 + 浏览器 Web Admin”。

## 17. 后续优先级

建议暂停继续堆更多 reviewer 功能，先做一轮语义收敛。

### P0：文档与产品语义收敛

- 修 README 和规划文档；
- 明确 reviewer 实际调用已实现；
- 明确 audit / guard / full-context 的边界；
- 将 full 从完成态改为实验性；
- 明确 streaming 只 audit；
- 明确 Web Admin + tray-only 方向。

### P1：拆分配置模型（已落地基础版）

当前代码已把 `mode: full` 收敛为拆分模型：

```json
{
  "mode": "audit" | "guard",
  "context": "response" | "full",
  "outboundReview": "off" | "audit" | "guard"
}
```

已完成：

- 配置解析支持 `context` 与 `outboundReview`。
- 旧 `mode: "full"` 保留兼容迁移，运行时归一化为 `mode: "guard" + context: "full" + outboundReview: "guard"`。
- ReviewJob 记录 `context` 与 `direction`。
- Admin UI Reviewer 页面已展示和保存拆分配置。
- README、规划、项目日志、示例配置和测试已同步。

### P2：修 outbound confirm/redact（已完成基础版）

出站审查不能只处理 block。

已完成：

- outbound `confirm` 不再静默放行：默认 `confirmBehavior: "queue"` 时返回 409 `confirmation_required`，创建 `direction: "outbound"` 的确认项，并且不调用上游；
- 用户 allow 后，客户端用相同请求加 `x-tinygateway-confirmation: <confirmationId>` 重试，网关跳过出站 reviewer 后再调用上游；
- `confirmBehavior: "hold"` 时，outbound `confirm` 会挂起原请求；allow 后再调用上游，block/timeout 均不会调用上游；
- pending / blocked / mismatch 的 outbound confirmation header 会在调用上游前返回明确错误；
- outbound `redact` 在请求体改写未实现前保守阻断，返回 403 `redaction_unsupported`，并写入 `request_blocked`；
- Confirmations 页面展示方向和请求/响应摘要。

仍未完成：

- outbound 请求体实际改写后再调用上游。

### P3：实现 hold confirmation（已完成基础版）

面向 Claude Code、Codex 等客户端，已实现：

```text
confirm -> hold original request -> user allow/block -> original request continues/errors
```

### P4：系统托盘控制器

实现 tray-only companion：

- 启动/停止/重启 TinyGateway；
- 打开 Web Admin；
- 打开日志目录；
- 显示运行状态；
- 开机自启。

不要把 Web Admin 塞进桌面 WebView。

## 17. 一句话结论

TinyGateway 的正确方向是：

```text
本地协议网关 + reviewer 结构化审查 + Web Admin 确认面板 + tray-only 控制器
```

`full` 应收敛为 `full-context` 标签，而不是独立执行模式。`audit` 和 `guard` 都可以启用 full-context。确认应优先做 hold 模式，避免把警告文本塞进 Claude Code、Codex 的正常模型输出里。
