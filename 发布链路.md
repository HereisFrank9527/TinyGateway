# TinyGateway 发布链路

本文记录便携版发布流程。当前维护重心是便携版，不引入桌面壳。

## 1. 修改版本号

版本号只改根目录：

```text
VERSION
```

示例：

```text
0.1.1
```

建议 GitHub Release tag 使用同一个版本并加 `v` 前缀：

```text
v0.1.1
```

## 2. 检查配置示例

如果本次改动涉及配置结构，同步维护：

```text
config.example.json
```

不要把真实 `config.json` 提交到仓库。

## 3. 跑测试

```powershell
npm test
```

必须通过后再发版。

## 4. 构建便携版

```powershell
npm run build:portable
```

产物：

```text
dist/TinyGateway
dist/TinyGateway-portable.zip
```

## 5. 创建 GitHub Release

仓库：

```text
https://github.com/HereisFrank9527/TinyGateway
```

创建 release：

```text
tag: v0.1.1
title: v0.1.1
```

上传资产：

```text
dist/TinyGateway-portable.zip
```

自动更新脚本会查找 latest release 中的 `TinyGateway-portable.zip`，或文件名包含 `portable` 的 zip。

## 5A. 让 Codex 执行自动发布

本机需要安装 GitHub CLI：

```powershell
winget install GitHub.cli
```

登录 GitHub：

```powershell
gh auth login
```

选择：

```text
GitHub.com
HTTPS
Login with a web browser
```

账号需要对仓库有写入权限：

```text
HereisFrank9527/TinyGateway
```

之后可以让 Codex 运行：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/release.ps1
```

脚本会执行：

1. 读取 `VERSION`，生成 tag，例如 `v0.1.1`。
2. 检查 `gh auth status`。
3. 运行 `npm test`。
4. 运行 `npm run build:portable`。
5. 提交 release 文件。
6. 创建 git tag。
7. push 分支和 tag。
8. 创建 GitHub Release。
9. 上传 `dist/TinyGateway-portable.zip`。

如果只想创建 draft release：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/release.ps1 -Draft
```

如果你已经手动提交，只想跳过提交：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/release.ps1 -NoCommit
```

注意：脚本不会提交 `config.json`、`logs/`、`dist/`、`规划.md`，这些已在 `.gitignore` 中忽略。

## 6. 验证更新

旧版便携目录里运行：

```text
update.bat
```

更新会保留：

```text
config.json
logs/
runtime/
```

然后覆盖程序文件。

更新后运行：

```text
start.bat
```

打开管理端，概览页会自动检查版本：

```text
http://127.0.0.1:8787/admin
```

## 7. 朴素启动仍然保留

项目根目录始终支持：

```powershell
npm start
node src/server.js
```

便携版只是额外分发形态。
